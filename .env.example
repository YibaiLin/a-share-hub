# ==========================================
# A-Share Hub 环境变量配置文件模板
# ==========================================
# 使用说明:
# 1. 复制此文件为 .env
# 2. 根据实际环境修改配置值
# 3. .env 文件不会被提交到Git (已在.gitignore中)
# ==========================================

# ------------------------------------------
# 应用基础配置
# ------------------------------------------
APP_NAME="A-Share Hub"
DEBUG=false
LOG_LEVEL=INFO

# ------------------------------------------
# API服务配置
# ------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000

# ------------------------------------------
# ClickHouse数据库配置
# ------------------------------------------
# 注意: WSL2环境可能需要使用实际IP而非localhost
# 端口说明: 8123=HTTP接口, 9000=Native TCP接口
# clickhouse-connect库使用HTTP接口，因此使用8123端口
CLICKHOUSE__HOST=localhost
CLICKHOUSE__PORT=8123
CLICKHOUSE__DATABASE=a_share
CLICKHOUSE__USER=default
CLICKHOUSE__PASSWORD=

# ------------------------------------------
# Redis缓存配置
# ------------------------------------------
REDIS__HOST=localhost
REDIS__PORT=6379
REDIS__DB=0
REDIS__PASSWORD=

# ------------------------------------------
# 数据采集器配置
# ------------------------------------------
# 采集延迟（秒）- API限流保护
COLLECTOR_DELAY=0.5

# 重试次数 - 网络异常时的重试次数
COLLECTOR_RETRY_TIMES=3

# 批量插入大小 - ClickHouse批量写入的记录数
COLLECTOR_BATCH_SIZE=1000